{
  "name": "uil-science-platform-root",
  "version": "0.1.0",
  "private": true,
  "description": "Root entry so hosts that build from the repo root still work. The app lives in /server.",
  "scripts": {
    "start": "node server/server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "express": "^4.19.2"
  },
  "engines": { "node": ">=18 <23" }
}
